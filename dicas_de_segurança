<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>🔒 Dicas de segurança</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Inter+Tight:wght@600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0d1117;           /* page background */
      --surface:#0f1623;      /* panel bg */
      --brand:#7c5cff;        /* accent */
      --text:#e6e6e6;         /* primary text */
      --muted:#a8b3cf;        /* secondary */
      --ok:#10b981;           /* success */
      --warn:#f59e0b;         /* warning */
      --danger:#ef4444;       /* danger */
      --shadow:0 14px 36px rgba(0,0,0,.35);
      --radius:16px;
      --device-w: 643.8px;    /* do Flutter */
      --device-h: 748.9px;    /* do Flutter */
    }
    *{box-sizing:border-box}
    html,body{height:100%; min-height:100vh}
    body{
      margin:0; background: radial-gradient(1200px 900px at 10% 0%, #141c2b 0%, transparent 60%),
                                 radial-gradient(1000px 800px at 100% 90%, #131a27 0%, transparent 55%),
                                 var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      display:grid; place-items:center; padding: 18px;
    }
    .device{ width:min(var(--device-w), 96vw); height:min(var(--device-h), 96vh); background:#1a2030; border-radius:26px; box-shadow:var(--shadow); overflow:hidden; display:flex; flex-direction:column }

    /* Header */
    .header{ background: var(--brand); color:#0b0d10; display:flex; align-items:center; justify-content:space-between; padding:14px 16px }
    .header h1{ margin:0; font: 700 20px/1.2 Inter Tight, Inter, sans-serif; color:#fff }
    .icon-btn{ width:36px; height:36px; display:grid; place-items:center; border-radius:10px; background: rgba(0,0,0,.2); border: 1px solid rgba(255,255,255,.25); cursor:pointer }
    .ico{ width:20px; height:20px; color:#fff }

    /* Content */
    .content{ flex:1; padding: 16px; overflow:auto; display:grid; grid-template-rows: auto auto 1fr; gap:16px }

    /* Tips list */
    .card{ background: var(--surface); border:1px solid rgba(255,255,255,.08); border-radius: var(--radius); padding: 16px; box-shadow: 0 8px 24px rgba(0,0,0,.25) }
    .card h2{ margin: 0 0 10px; font: 700 18px/1.2 Inter Tight, Inter, sans-serif }
    .list{ list-style:none; margin:0; padding:0; display:grid; gap:12px }
    .item{ display:flex; gap:10px; align-items:flex-start; background:#0e1522; border:1px solid rgba(255,255,255,.06); padding:12px 12px; border-radius:12px }
    .bullet{ flex:0 0 24px; width:24px; height:24px; display:grid; place-items:center; border-radius:8px; background:var(--ok); color:#001; font-weight:800 }
    .warn .bullet{ background:var(--warn); color:#111 }
    .danger .bullet{ background:var(--danger); color:#fff }
    .item p{ margin:2px 0 0; line-height:1.45 }

    /* Callout */
    .callout{ background: #201a30; border: 1px solid rgba(124,92,255,.35); border-radius: 12px; padding: 12px 14px; color: var(--muted) }

    /* Actions */
    .actions{ display:flex; gap:10px; flex-wrap:wrap }
    .btn{ height:40px; display:inline-flex; align-items:center; justify-content:center; gap:8px; background:#111827; color:#fff; border:none; border-radius:10px; font: 600 14px/1 Inter Tight, Inter, sans-serif; padding: 0 14px; cursor:pointer; box-shadow: 0 10px 24px rgba(0,0,0,.2) }
    .btn.brand{ background: var(--brand) }

    /* Toast */
    .toast{ position: fixed; inset: auto 50% 20px auto; transform: translateX(50%); background:#10151f; border:1px solid rgba(255,255,255,.08); color:#e6e6e6; padding:10px 14px; border-radius: 10px; box-shadow: var(--shadow); font: 500 13px/1.3 Inter, sans-serif; opacity:0; pointer-events:none; transition:.25s; z-index:60 }
    .toast.show{ opacity:1 }

    @media (max-height: 720px){
      .btn{ height:36px }
      .header h1{ font-size:18px }
    }
  </style>
</head>
<body>
  <div class="device">
    <div class="header">
      <h1>🔒 Dicas de segurança</h1>
      <button class="icon-btn" id="closeBtn" aria-label="Fechar">
        <svg class="ico" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M6 6l12 12M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
      </button>
    </div>

    <main class="content">
      <section class="card">
        <h2>Boas práticas essenciais</h2>
        <ul class="list">
          <li class="item warn"><span class="bullet">!</span><p><b>Nunca pague nada</b> antes de <b>visitar o imóvel</b> e confirmar a autenticidade do anúncio.</p></li>
          <li class="item"><span class="bullet">✓</span><p>Verifique se <b>proprietário/corretor</b> está devidamente identificado e com contatos verificáveis.</p></li>
          <li class="item"><span class="bullet">✓</span><p><b>Leia o contrato</b> por completo antes de assinar. Tire dúvidas sobre multas, reajustes e prazos.</p></li>
          <li class="item danger"><span class="bullet">✕</span><p>Evite negociar <b>fora das plataformas seguras</b> e desconfie de pressa incomum para fechar negócio.</p></li>
        </ul>
      </section>

      <section class="card">
        <h2>Checklist rápido da visita</h2>
        <ul class="list">
          <li class="item"><span class="bullet">✓</span><p>Teste torneiras, chuveiro, energia, gás e trancas.</p></li>
          <li class="item"><span class="bullet">✓</span><p>Observe sinais de infiltração, mofo e fissuras.</p></li>
          <li class="item"><span class="bullet">✓</span><p>Converse com vizinhos sobre segurança e barulho.</p></li>
        </ul>
        <div class="actions" style="margin-top:12px">
          <button class="btn brand" id="copyChecklist">Copiar checklist</button>
        </div>
      </section>

      <div class="callout">Dica: Fotografe o estado do imóvel durante a visita e anexe as fotos ao contrato de locação.</div>
    </main>
  </div>

  <div class="toast" id="toast" role="status" aria-live="polite"></div>

  <script>
    const qs = (s, el=document)=> el.querySelector(s);

    // Close
    qs('#closeBtn').addEventListener('click', ()=>{
      const dev = qs('.device');
      dev.style.transform = 'scale(.98) translateY(8px)';
      dev.style.opacity = '0';
      setTimeout(()=> dev.remove(), 180);
    });

    // Toast
    const toast = qs('#toast');
    function showToast(msg){ toast.textContent = msg; toast.classList.add('show'); clearTimeout(showToast._t); showToast._t = setTimeout(()=> toast.classList.remove('show'), 1600); }

    // Copy checklist
    qs('#copyChecklist').addEventListener('click', async ()=>{
      const text = [
        'Visitar o imóvel presencialmente',
        'Testar água, energia, gás e trancas',
        'Verificar infiltrações/mofo/fissuras',
        'Conversar com vizinhos (segurança/barulho)'
      ].map((t,i)=> ${i+1}. ${t}).join('\n');
      try{ await navigator.clipboard.writeText(text); showToast('Checklist copiado 👍'); }
      catch{ showToast('Não foi possível copiar'); }
    });
  </script>
</body>
</html>
