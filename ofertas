<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>💸 Ofertas de Imóveis</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Inter+Tight:wght@600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0d1117;           /* page background */
      --panel:#0f1623;        /* cards */
      --brand:#7c5cff;        /* accent */
      --text:#e6e6e6;         /* primary text */
      --muted:#a8b3cf;        /* secondary */
      --shadow:0 14px 36px rgba(0,0,0,.35);
      --radius:16px;
      --device-w: 556.4px;    /* do Flutter */
      --device-h: 896.3px;    /* do Flutter */
    }
    *{box-sizing:border-box}
    html,body{height:100%; min-height:100vh}
    body{
      margin:0; background: radial-gradient(1200px 900px at 10% 0%, #141c2b 0%, transparent 60%),
                                 radial-gradient(1000px 800px at 100% 90%, #131a27 0%, transparent 55%),
                                 var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      display:grid; place-items:center; padding: 18px;
    }
    .device{ width:min(var(--device-w), 96vw); height:min(var(--device-h), 96vh); background:#1a2030; border-radius:26px; box-shadow:var(--shadow); overflow:hidden; display:flex; flex-direction:column }

    /* Header */
    .header{ background: var(--brand); display:flex; align-items:center; justify-content:space-between; padding:14px 16px }
    .header h1{ margin:0; font: 700 20px/1.2 Inter Tight, Inter, sans-serif; color:#fff }
    .icon-btn{ width:36px; height:36px; display:grid; place-items:center; border-radius:10px; background: rgba(0,0,0,.2); border: 1px solid rgba(255,255,255,.25); cursor:pointer }
    .ico{ width:20px; height:20px; color:#fff }

    /* Content */
    .content{ flex:1; padding: 14px; overflow:auto; display:grid; grid-template-rows: auto auto auto auto 1fr; gap:14px }

    /* Promo title */
    .promo-title{ font: 700 26px/1.2 Work Sans, Inter, system-ui; color: var(--brand); text-align:center }

    /* Info boxes */
    .info{ background: rgba(255,255,255,.1); border:1px solid rgba(255,255,255,.15); border-radius:12px; padding:12px 14px; font: 600 13px/1.5 Inter, sans-serif }

    /* Carousel (CSS scroll-snap + JS helpers) */
    .carousel{ position:relative; background: var(--panel); border:1px solid rgba(255,255,255,.08); border-radius:12px; padding:12px }
    .track{ display:flex; gap:12px; overflow:auto hidden; scroll-snap-type: x mandatory; scroll-behavior:smooth; padding-bottom: 4px }
    .card{ flex:0 0 70%; max-width:70%; height:140px; border-radius:12px; overflow:hidden; position:relative; scroll-snap-align:center; box-shadow: 0 8px 24px rgba(0,0,0,.25); transform: scale(.94); transition:.25s }
    .card img{ width:100%; height:100%; object-fit:cover }
    .track:has(.card:hover) .card{ transform: scale(.90); }
    .card:hover{ transform: scale(.98); }

    .nav{ position:absolute; inset: 0; display:flex; align-items:center; justify-content:space-between; pointer-events:none }
    .nav button{ pointer-events:auto; width:36px; height:36px; border:none; border-radius:50%; background:rgba(0,0,0,.4); color:#fff; display:grid; place-items:center; cursor:pointer }

    /* Footer spacer */
    .sp{ height:4px }

    @media (min-width: 520px){ .card{ flex-basis:50%; max-width:50% } }
    @media (max-height: 760px){ .header h1{ font-size:18px } .card{ height:126px } }
  </style>
</head>
<body>
  <div class="device">
    <div class="header">
      <h1>💸 Ofertas de Imóveis</h1>
      <button class="icon-btn" id="closeBtn" aria-label="Fechar">
        <svg class="ico" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M6 6l12 12M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
      </button>
    </div>

    <main class="content">
      <div class="promo-title">Promoção Imperdível!</div>

      <!-- Carrossel #1 -->
      <section class="carousel" id="c1">
        <div class="track">
          <div class="card"><img src="https://images.unsplash.com/photo-1505691938895-1758d7feb511?q=80&w=800&auto=format&fit=crop" alt="kitnet 1"/></div>
          <div class="card"><img src="https://images.unsplash.com/photo-1494526585095-c41746248156?q=80&w=800&auto=format&fit=crop" alt="kitnet 2"/></div>
          <div class="card"><img src="https://images.unsplash.com/photo-1502005229762-cf1b2da7c52f?q=80&w=800&auto=format&fit=crop" alt="kitnet 3"/></div>
        </div>
        <div class="nav">
          <button data-dir="-1" aria-label="Anterior">‹</button>
          <button data-dir="1" aria-label="Próximo">›</button>
        </div>
      </section>

      <div class="info">
        <b>Kitnet Compacta (15–25 m²)</b><br>
        💰 R$ 800 – R$ 1.200/mês<br>
        <b>Promoção:</b> Primeiro mês com 20% de desconto → R$ 640 – R$ 960
        <hr style="border:none;border-top:1px solid rgba(255,255,255,.15); margin:10px 0">
        <b>Kitnet Média (25–35 m²)</b><br>
        💰 R$ 1.200 – R$ 1.600/mês<br>
        <b>Promoção:</b> Assine agora e pague R$ 1.000 no primeiro mês
      </div>

      <!-- Carrossel #2 -->
      <section class="carousel" id="c2">
        <div class="track">
          <div class="card"><img src="https://images.unsplash.com/photo-1501183638710-841dd1904471?q=80&w=800&auto=format&fit=crop" alt="apto 1"/></div>
          <div class="card"><img src="https://images.unsplash.com/photo-1505691723518-36a5ac3b2d52?q=80&w=800&auto=format&fit=crop" alt="apto 2"/></div>
          <div class="card"><img src="https://images.unsplash.com/photo-1507120410856-1f35574c3b45?q=80&w=800&auto=format&fit=crop" alt="apto 3"/></div>
        </div>
        <div class="nav">
          <button data-dir="-1" aria-label="Anterior">‹</button>
          <button data-dir="1" aria-label="Próximo">›</button>
        </div>
      </section>

      <div class="info">
        <b>Apartamento 1 Quarto (35–50 m²)</b><br>
        💰 R$ 1.500 – R$ 2.200/mês<br>
        <b>Promoção:</b> Desconto de R$ 300 no primeiro mês
        <hr style="border:none;border-top:1px solid rgba(255,255,255,.15); margin:10px 0">
        <b>Apartamento 2 Quartos (50–70 m²)</b><br>
        💰 R$ 2.200 – R$ 3.000/mês<br>
        <b>Promoção:</b> Assinando até o final do mês → primeiro mês pela metade do preço
      </div>

      <div class="sp"></div>
    </main>
  </div>

  <script>
    const qs=(s,el=document)=>el.querySelector(s);
    const qsa=(s,el=document)=>[...el.querySelectorAll(s)];

    // Close
    qs('#closeBtn').addEventListener('click', ()=>{
      const dev = qs('.device');
      dev.style.transform = 'scale(.98) translateY(8px)';
      dev.style.opacity = '0';
      setTimeout(()=> dev.remove(), 180);
    });

    // Carousel helpers
    function makeCarousel(root){
      const track = qs('.track', root);
      const cards = qsa('.card', track);
      const btns = qsa('.nav button', root);
      let i = 0;
      const step = ()=> track.clientWidth * 0.7 + 12; // card width + gap
      const clamp = v=> Math.max(0, Math.min(v, cards.length-1));
      const go = (idx)=>{ i = clamp(idx); track.scrollTo({left: i*step(), behavior:'smooth'}); };
      btns[0].addEventListener('click', ()=> go(i-1));
      btns[1].addEventListener('click', ()=> go(i+1));
      // Drag wheel (shift+wheel for horizontal)
      root.addEventListener('wheel', (e)=>{ if(Math.abs(e.deltaX) < Math.abs(e.deltaY)) return; e.preventDefault(); track.scrollLeft += e.deltaX; }, {passive:false});
      // Resize recalibration
      addEventListener('resize', ()=> go(i));
      // Auto center on load
      setTimeout(()=> go(1), 150);
    }

    makeCarousel(qs('#c1'));
    makeCarousel(qs('#c2'));
  </script>
</body>
</html>
