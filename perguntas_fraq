<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>❓ Perguntas Frequentes (FAQ)</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Inter+Tight:wght@600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0d1117;           /* page background */
      --surface:#0f1623;      /* panel bg */
      --brand:#7c5cff;        /* accent */
      --text:#e6e6e6;         /* primary text */
      --muted:#a8b3cf;        /* secondary */
      --shadow:0 14px 36px rgba(0,0,0,.35);
      --radius:16px;
      --device-w: 643.8px;    /* do Flutter */
      --device-h: 748.9px;    /* do Flutter */
    }
    *{box-sizing:border-box}
    html,body{height:100%; min-height:100vh}
    body{
      margin:0; background: radial-gradient(1200px 900px at 10% 0%, #141c2b 0%, transparent 60%),
                                 radial-gradient(1000px 800px at 100% 90%, #131a27 0%, transparent 55%),
                                 var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial;
      display:grid; place-items:center; padding: 18px;
    }
    .device{ width:min(var(--device-w), 96vw); height:min(var(--device-h), 96vh); background:#1a2030; border-radius:26px; box-shadow:var(--shadow); overflow:hidden; display:flex; flex-direction:column }

    /* Header */
    .header{ background: var(--brand); display:flex; align-items:center; justify-content:space-between; padding:14px 16px }
    .header h1{ margin:0; font: 700 20px/1.2 Inter Tight, Inter, sans-serif; color:#fff }
    .icon-btn{ width:36px; height:36px; display:grid; place-items:center; border-radius:10px; background: rgba(0,0,0,.2); border: 1px solid rgba(255,255,255,.25); cursor:pointer }
    .ico{ width:20px; height:20px; color:#fff }

    /* Content */
    .content{ flex:1; padding: 16px; overflow:auto; display:grid; grid-template-rows: auto 1fr auto; gap:12px }

    /* Search */
    .search{ position:relative }
    .search input{ width:100%; height:44px; padding:0 12px 0 44px; border-radius:12px; border:1px solid rgba(255,255,255,.14); background:#0f1623; color:var(--text); font-weight:600 }
    .search .in-icon{ position:absolute; left:12px; top:50%; transform:translateY(-50%); width:20px; height:20px; color:#cbd5e1; opacity:.9 }

    /* FAQ */
    .faq{ display:grid; gap:10px }
    .qa{ border-radius:12px; background:#0e1522; border:1px solid rgba(255,255,255,.08); overflow:hidden }
    .q{ display:flex; align-items:center; gap:10px; padding:12px 14px; cursor:pointer; user-select:none }
    .q h3{ margin:0; font:700 15px/1.2 Inter, sans-serif }
    .q .chev{ margin-left:auto; transition:.25s; width:18px; height:18px; color:#cbd5e1 }
    .a{ display:none; padding: 0 14px 12px; color: var(--muted) }
    .qa.open .a{ display:block }
    .qa.open .q .chev{ transform: rotate(180deg) }

    /* Footer actions */
    .actions{ display:flex; gap:10px; flex-wrap:wrap }
    .btn{ height:40px; display:inline-flex; align-items:center; justify-content:center; gap:8px; background:#111827; color:#fff; border:none; border-radius:10px; font: 600 14px/1 Inter Tight, Inter, sans-serif; padding: 0 14px; cursor:pointer; box-shadow: 0 10px 24px rgba(0,0,0,.2) }
    .btn.brand{ background: var(--brand) }

    /* Toast */
    .toast{ position: fixed; inset: auto 50% 20px auto; transform: translateX(50%); background:#10151f; border:1px solid rgba(255,255,255,.08); color:#e6e6e6; padding:10px 14px; border-radius: 10px; box-shadow: var(--shadow); font: 500 13px/1.3 Inter, sans-serif; opacity:0; pointer-events:none; transition:.25s; z-index:60 }
    .toast.show{ opacity:1 }

    @media (max-height: 720px){ .btn{ height:36px } .header h1{ font-size:18px } }
  </style>
</head>
<body>
  <div class="device">
    <div class="header">
      <h1>❓ Perguntas Frequentes (FAQ)</h1>
      <button class="icon-btn" id="closeBtn" aria-label="Fechar">
        <svg class="ico" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M6 6l12 12M18 6L6 18" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
      </button>
    </div>

    <main class="content">
      <!-- Busca -->
      <div class="search">
        <input id="filtro" placeholder="Buscar no FAQ (ex.: fiador, visita, contas)" />
        <svg class="in-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M21 21l-4.35-4.35M10 18a8 8 0 100-16 8 8 0 000 16z" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
      </div>

      <!-- Lista FAQ -->
      <section class="faq" id="faq">
        <article class="qa open">
          <div class="q"><h3>Preciso de fiador para alugar?</h3><svg class="chev" viewBox="0 0 20 20" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M5.23 7.21a.75.75 0 011.06.02L10 11.06l3.71-3.83a.75.75 0 111.08 1.04l-4.25 4.39a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z"/></svg></div>
          <div class="a"><p>Depende do proprietário. Algumas opções aceitam <b>caução</b> ou <b>seguro-fiança</b>.</p></div>
        </article>

        <article class="qa">
          <div class="q"><h3>Posso visitar antes de assinar?</h3><svg class="chev" viewBox="0 0 20 20" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M5.23 7.21a.75.75 0 011.06.02L10 11.06l3.71-3.83a.75.75 0 111.08 1.04l-4.25 4.39a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z"/></svg></div>
          <div class="a"><p>Sim. A visita é essencial para confirmar as condições do imóvel.</p></div>
        </article>

        <article class="qa">
          <div class="q"><h3>O aluguel inclui contas de água e luz?</h3><svg class="chev" viewBox="0 0 20 20" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M5.23 7.21a.75.75 0 011.06.02L10 11.06l3.71-3.83a.75.75 0 111.08 1.04l-4.25 4.39a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z"/></svg></div>
          <div class="a"><p>Varia de imóvel para imóvel. Verifique a descrição ou confirme com o proprietário.</p></div>
        </article>

        <article class="qa">
          <div class="q"><h3>Como denunciar um anúncio falso?</h3><svg class="chev" viewBox="0 0 20 20" fill="currentColor" xmlns="http://www.w3.org/2000/svg"><path d="M5.23 7.21a.75.75 0 011.06.02L10 11.06l3.71-3.83a.75.75 0 111.08 1.04l-4.25 4.39a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z"/></svg></div>
          <div class="a"><p>Use o canal de suporte da plataforma e informe os detalhes do anúncio.</p></div>
        </article>
      </section>

      <!-- Ações -->
      <div class="actions">
        <button class="btn brand" id="expandAll">Expandir tudo</button>
        <button class="btn" id="collapseAll">Recolher tudo</button>
        <button class="btn" id="copyFaq">Copiar FAQ</button>
      </div>
    </main>
  </div>

  <div class="toast" id="toast" role="status" aria-live="polite"></div>

  <script>
    const qs = (s, el=document)=> el.querySelector(s);
    const qsa = (s, el=document)=> [...el.querySelectorAll(s)];

    // Close
    qs('#closeBtn').addEventListener('click', ()=>{
      const dev = qs('.device');
      dev.style.transform = 'scale(.98) translateY(8px)';
      dev.style.opacity = '0';
      setTimeout(()=> dev.remove(), 180);
    });

    // Toast
    const toast = qs('#toast');
    function showToast(msg){ toast.textContent = msg; toast.classList.add('show'); clearTimeout(showToast._t); showToast._t = setTimeout(()=> toast.classList.remove('show'), 1600); }

    // Toggle items
    qsa('.qa .q').forEach(q=>{
      q.addEventListener('click', ()=> q.parentElement.classList.toggle('open'));
    });

    // Expand/Collapse
    qs('#expandAll').addEventListener('click', ()=>{ qsa('.qa').forEach(e=> e.classList.add('open')); });
    qs('#collapseAll').addEventListener('click', ()=>{ qsa('.qa').forEach(e=> e.classList.remove('open')); });

    // Copy FAQ
    qs('#copyFaq').addEventListener('click', async ()=>{
      const text = qsa('#faq .qa').map(card=>{
        const q = card.querySelector('.q h3')?.textContent?.trim()||'';
        const a = card.querySelector('.a')?.textContent?.trim()||'';
        return Q: ${q}\nA: ${a};
      }).join('\n\n');
      try{ await navigator.clipboard.writeText(text); showToast('FAQ copiado 👍'); }
      catch{ showToast('Não foi possível copiar'); }
    });

    // Filter
    const filtro = qs('#filtro');
    filtro.addEventListener('input', ()=>{
      const term = filtro.value.toLowerCase();
      qsa('#faq .qa').forEach(card=>{
        const txt = card.textContent.toLowerCase();
        card.style.display = txt.includes(term) ? '' : 'none';
      });
    });
  </script>
</body>
</html>
